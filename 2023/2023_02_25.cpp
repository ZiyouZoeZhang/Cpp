#include <iostream>
#include <vector>
#include <string>
using namespace std;

vector<int>nums;
vector<int>solution;

int main(){
    cin<<nums;
    

    for(int i=0; i<nums.size(); i++){
        //寻找与i长度相等的子集。？
        //第一个默认一样
        //然后前面不变，额，从后面推 (i-1的地方推)
        
        //if i==1 cout for (int j =0, int j<nums.size(); j++)
        //if i==2 cout 上面这个数组。。。每个数字加一个后面的元素
        //if i==3 cout ℹ==1这个数组，每个数组加上后面-最后一个元素
        //[2,3] [2,4] [3,4]
        //initialize
    }

    
}